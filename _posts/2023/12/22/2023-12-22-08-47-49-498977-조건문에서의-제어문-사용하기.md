---
layout: post
title: "[python] 조건문에서의 제어문 사용하기"
description: " "
date: 2023-12-22
tags: [python]
comments: true
share: true
---

Python에서 조건문을 사용하여 프로그램의 흐름을 제어할 수 있습니다. 이를 통해 특정 조건에 따라 다른 코드 블록을 실행하거나 건너뛸 수 있습니다.

## if 문

가장 기본적인 조건문은 `if` 문입니다. 아래는 `if` 문의 구조입니다.

```python
if 조건:
    # 조건이 참일 때 실행될 코드
else:
    # 조건이 거짓일 때 실행될 코드
```

## elif 문

여러 조건을 순차적으로 검사하려면 `elif` 문을 사용합니다. 아래는 `elif` 문의 구조입니다.

```python
if 조건1:
    # 조건1이 참일 때 실행될 코드
elif 조건2:
    # 조건2가 참일 때 실행될 코드
else:
    # 모든 조건이 거짓일 때 실행될 코드
```

## 조건문과 논리 연산자

조건식에 논리 연산자를 활용하여 여러 조건을 결합할 수 있습니다. 아래는 논리 연산자의 예시입니다.

```python
if 조건1 and 조건2:
    # 조건1과 조건2가 모두 참일 때 실행될 코드
elif 조건3 or 조건4:
    # 조건3 또는 조건4 중 하나라도 참일 때 실행될 코드
```

Python에서 조건문과 제어문을 사용하여 프로그램의 흐름을 유연하게 제어할 수 있습니다. 여기서 소개한 내용을 활용하여 다양한 상황에 대응할 수 있습니다.

더 많은 정보는 [Python 공식 문서](https://docs.python.org/3/tutorial/controlflow.html)를 참고하세요.